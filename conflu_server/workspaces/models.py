from django.db import models

# Create your models here.
# Note: The Space model, which might logically fit here,
# is currently defined in pages.models.py for simplicity during initial setup
# as the Page model has a ForeignKey to it. It can be moved here later
# if the project structure evolves.
